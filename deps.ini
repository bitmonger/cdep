[toolchain]
cmd = ./toolchain/build.sh

[kernel]
cmd = ./kernel/build.sh
dep = toolchain

[bootloader]
cmd = ./bootloader/build.sh
dep = toolchain

[userspace]
cmd = ./userspace/build.sh
dep = toolchain
dep = kernel
dep = bootloader
